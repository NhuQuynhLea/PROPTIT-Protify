<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:id="@+id/song_layout">

    <TextView
        android:id="@+id/song_title_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/album_title"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/download_btn"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/shape_circle"
        android:src="@drawable/ic_download"
        app:layout_constraintEnd_toStartOf="@+id/artist_name_txt"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintStart_toStartOf="@+id/song_title_txt"
        app:layout_constraintTop_toBottomOf="@+id/song_title_txt" />

    <TextView
        android:id="@+id/artist_name_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:text="@string/artist_name"
        app:layout_constraintBottom_toBottomOf="@id/download_btn"
        app:layout_constraintStart_toEndOf="@+id/download_btn"
        app:layout_constraintTop_toTopOf="@id/download_btn" />

    <ImageView
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/ic_option"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/artist_name_txt"
        />

</androidx.constraintlayout.widget.ConstraintLayout>